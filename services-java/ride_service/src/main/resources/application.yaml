server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ride_sharing_app?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root # Change this to your actual MySQL password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # Automatically creates/updates tables based on Ride.java
    show-sql: true
   